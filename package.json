{
  "name": "pomodoro",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "lint": "eslint src/**",
    "build": "browserify -t babelify -d src/index.js -o app/js/pomodoroClock.js",
    "watch": "watch \"npm run build\" src",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "K. P. MacGregor <phoenixstormcrow@gmail.com>",
  "license": "MIT",
  "devDependencies": {
    "babelify": "^6.1.3",
    "browserify": "^10.2.6"
  }
}
